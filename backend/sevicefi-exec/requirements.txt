# requirements must be compatible with python 3.13.3
# Core dependencies for iExec protected data deserialization
borsh-construct==0.1.0

# No additional dependencies needed - credit scoring uses only stdlib
# json, hashlib, datetime, dataclasses, enum are all built-in
